rootProject.name = "bank-project"

// Incluindo builds do diretório comum
includeBuild("0-common/extensions")
includeBuild("0-common/libs")

// Incluindo o serviço de user no projeto
includeBuild("1-app/user")
